import React, {  } from 'react';
// import noteContext from '../context/notes/NoteContext';

const About = () => {
  // const a = useContext(noteContext)xx  
  return (
    <div>This is About </div>  // Ensure there is a space between "About" and "{a.name}"
  );
}

export default About;
